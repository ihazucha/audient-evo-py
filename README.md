# Audient EVO 4 - reverse engineered Linux controller

## TODO

1. implement input select and phantom power (mute is done)
2. package it
3. DKMS setup for kernel module auto-rebuild on kernel upgrades
  - figure out how it works, make proper package name, document

## EVO 4 Specification

Functions:
- input select
  - gain knob
  - mute toggle
  - phantom power toggle
- monitor mix
  - input/output ratio knob
- output volume
  - volume knob
  - mute toggle

HW:
- USB:
  - VID: 0x2708
  - PID: 0x0006

SW (Vendor control app):
- Windows/MacOS only (this project replaces it on Linux)

## Installation

Requirements:
- Linux with snd-usb-audio (standard kernel module for audio)
- evo4_raw kernel module (see `kmod/`) — for mixer/mute controls via `/dev/evo4`
- Python 3.10+

## USBPcap
- Host -> Dev (SET CUR) - generated by changes in app:
  usb.bmRequestType == 0x21
- Host -> Dev (GET CUR/MEM) - device state polling:
  usb.bmRequestType == 0xA1

## Dev Workflow

1. On Windows:
  - run EVO 4 App
  - capture comms using USBPcap, filter in Wireshark
  - map requests to functions (volume, mute, ...)
2. Update `audient.py`, implement functionality, test.
  - the device sends info to host about the value set by a request
  - `audient.py` should also implement a poll (GET) requests to a function
